KATHMANDU UNIVERSITY
End Semester Examination

2013
Course: COMP 103
Year: I Semester: I
SECTION “B”


1. Describe different steps for the development of a C program.

2. With example, describe any four types of operators used in C programming.

3. What is the importance of loop structure? With example, describe do-while loop, while loop, and for loop.

4. Write a program to check whether a given number is Armstrong or not.  
   [Hint: XYZ is an Armstrong number because X³ + Y³ + Z³ = XYZ]

5. Define a string. With example, describe any six string manipulating functions.

6. Using pointer, write a program to calculate the average of user-given number of array elements.  
   Ensure that the average function returns a floating point value.

7. Write a C program to swap (interchange) values of two variables with the help of function and pointer.

SECTION “C”


8. Write a C program that illustrates how an array of structures is passed to a function, and how a pointer to a particular structure is returned.

9. Differentiate pass by value with pass by reference. Write a program to illustrate three library functions related to dynamic memory allocation in C programming.

10. Briefly explain different types of storage class specifiers that are used in C programming language.  
    Write a C program to copy and compare structure variables.


KATHMANDU UNIVERSITY
End Semester Examination [C]

2014
Course: COMP 103
Year: I Semester: I
SECTION “B”

1. Summarize the rules for naming identifiers. Are uppercase letters equivalent to lowercase letters? Name the different classes of statements in C. Describe the composition of each.

2. Describe the output that will be generated by each of the following C programs:  

   a.  #include <stdio.h>
   int main() {
       int i, j, x = 0;
       for(i = 0; i < 5; ++i)
           for(j = 0; j < i; ++j) {
               x += (i + j - 1);
               printf("%d", x);
           }
       printf("\nx = %d", x);
       return 0;
   }
   b.#include <stdio.h>
int funct1(int n) {
    if(n > 0) return n + funct1(n - 1);
    else return 0;
}
int main() {
    int n = 10;
    printf("%d", funct1(n));
    return 0;
}
3. What is meant by branching? Suppose a break statement is included within the innermost of several nested control statements. What happens when the break statement is executed?

4. Write a loop that will generate every fourth integer, beginning with variable i = 3 and containing all integers less than 150. Calculate the sum of those integers that are divisible by 7.

5. Write a program to swap values of two variables using a function. Pass the arguments to the function by reference.

6. Write a program to read integers n1 and n2 and display all odd numbers between those two numbers.

7. What is the purpose of the keyword void? Where is this keyword used? Why might a return statement be included in a function that does not return any value?
SECTION “C”


8. What are the keywords in C? What restrictions apply to their use? Compare the use of the switch statement with nested if-else statements. What is a function? Are functions required when writing a C program? State three advantages of using functions. Can the names of formal arguments within a function coincide with the names of other variables defined outside of the function? Explain.

9. In what way does an array differ from an ordinary variable? How are individual array elements identified? What advantages are there in defining an array size in terms of a symbolic constant rather than a fixed integer? What value is automatically assigned to array elements that are not explicitly initialized? Write a program that reads two matrices of order m x n and p x q respectively using a function readMatrix(). The program should contain a function processMatrix() that multiplies the matrices. Display the result using a function showMatrix().

10. Write a program that defines a structure STUDENT containing name, symbol number, marks of six subjects, total mark, and percentage as members. Read the data for ten students. Display the records in descending order according to the percentage obtained by the students.

KATHMANDU UNIVERSITY
End-Semester Examination
2015

Course: COMP 103
Year: I Semester: I
SECTION “B”


1. Define structured programming language. Describe the general structure of a C program.

2. What do you mean by data type? With example, describe three classes of data types.

3. Define decision structure. Write a program to illustrate switch-case, continue, and break statements.

4. Write a C program to check whether a number is a Strong Number or not.
[Hint: 145 is a strong number because 1! + 4! + 5! = 145]

5. Write a program that reads twelve numbers entered by the user and prints if any of them match. With example, differentiate pass by value with pass by reference.

6. Define dynamic memory allocation and briefly explain different dynamic memory allocation functions.

SECTION “C”


8. Using a function, write a program to multiply two matrices. Display the resultant matrix. Write a C program that illustrates how an array of structures is passed to a function, and how a pointer to a particular structure is returned.

9. Explain storage class specifiers. Write a program to enter the records of n students according to the structure given below and arrange the records in ascending order based on their names:
struct student {
    int Roll_Num;
    char Name[20];
    char Dept[20];
};

KATHMANDU UNIVERSITY
End Semester Examination
2016

Course: COMP 102
Year: I Semester: II
SECTION “B”


1. Explain the following loop control statements in C with example:  
   (a) `break` statement  
   (b) `continue` statement  
   (c) `switch` statement  

   Distinguish between `#include` and `#define`.

2. What is a character array? How is it different from other data types of arrays?  
   Write a C program to find the smallest value among four numbers.

3. Define different types of statements used in C.

4. Define `for` loop. Write a C program to find the sum and average of given numbers using a `for` loop.

5. Explain the difference between `printf()` and `scanf()` with examples. Write a program in C to find the principal, rate of interest, and number of years.

SECTION “C”


8. What is a multidimensional array? How is it different from a one-dimensional array?  
   Write a C program to read the elements of two matrices of order n x n and perform matrix multiplication.

9. Distinguish a structure data type from other data type variables. Develop a program in C to read the following information from the keyboard:  
   - Employee's name  
   - Employee's code  
   - Designation  
   - Years of experience  
   - Age



KATHMANDU UNIVERSITY
End Semester Examination
2016

Course: COMP 103
Year: I Semester: I
SECTION “B”


1. Describe the usefulness of the `switch` statement. Write a program that reads a value x and evaluates the following functions using nested `if` statements:  
   - y = 1 for x > 0  
   - y = 0 for x = 0  
   - y = -1 for x < 0

2. What are relational operators? Provide a C snippet that demonstrates the use of the `!=` (not-equal-to) operator. How does `==` differ from `!=`?

3. Describe the syntax of `do-while` statement. The time period of a pendulum can be calculated using the following equation:
 T = 2π * sqrt(L / g)
where L is the length of the pendulum, g is the acceleration due to gravity (= 9.8).  
Write a program to calculate the time period of a simple pendulum for values of L from 1 to 10 in steps of 1.

4. Consider the weekly salary of a salesman selling some domestic products. If x is the number of products sold in a week, his weekly salary is given by:  
 salary = 100 for x <= 20
 salary = 300 for x <= 40
 salary = 4.5*x + 150 for x > 40  
Write a function `get_salary` that takes x as an argument and returns the salary. Also write a complete program using this function.

5. What is an array? How does passing a variable to a function differ from passing an array to a function?  
What are static storage classes? Write a complete C program that shows the use of a static variable.

6. Explain the differences between `malloc()` and `calloc()` functions in dynamic memory allocation.

SECTION “C”


8. Given a square matrix A of size 10 x 10, write a program that identifies whether the matrix is diagonal.  
(A diagonal matrix has all non-diagonal elements equal to zero.)

9. Create a structure called `Employee` with attributes: name, salary, year, and taxable;  
- `name` stores the employee's name  
- `year` stores the year of joining the company  
- `salary` stores the annual income  
- `taxable` stores either 0 or 1 (0 if salary is not taxable, 1 if taxable)  

In the main program, create an array of 5 employees. Initialize the array and calculate the total tax collected from all employees (tax rate = 15%).

10. Consider a student with grades in 5 subjects. Scores of 4 subjects are arranged in an array:  
Score[4] = {86, 45, 51, 61}
The highest mark is 100 and the minimum passing score is 40. Write a program to calculate the score required in the 5th subject to achieve a given percentage. Show the following test cases:  
- Test Case #1: Enter percentage = 78 → Impossible to score  
- Test Case #2: Enter percentage = 60 → Mark required = 57  
- Test Case #3: Enter percentage = 45 → Minimum percentage achievable = 56.6



KATHMANDU UNIVERSITY
End Semester Examination
2016

Course: COMP 103
Year: I Semester: I
SECTION “B”

1. Explain the four steps of development of a C program.

2. Write a program in C to illustrate `switch-case` statement.

3. What do you mean by control structure? With example, explain the two types of repetition control.

4. Using a function, write a program to demonstrate recursion.

5. With example, differentiate pass by value with pass by reference.

6. With example, explain nested loops and nested structures.

7. Write a program to calculate the average of user-given number of array elements. Ensure the average function returns a floating point value.

SECTION “C”


8. Write a C program that finds the transpose of a given 3 x 3 matrix. Display both the original and transposed matrices using functions.

9. With example, define `typedef` and `union`. Using structures and functions, write a C program to sort information of five students by their names.

10. Compare and contrast an array with a structure. Define storage class. Explain different types of storage classes.


KATHMANDU UNIVERSITY
End Semester Examination
2017

Course: COMP 102
Year: I Semester: II
SECTION “B”


1. What is a variable? What sorts of variable names are acceptable in C programming? How does it differ from a keyword?

2. What do you mean by looping? Write a program to print all even numbers between 101 and 201, except those divisible by 14.

3. Describe a static variable in terms of its scope and lifetime. Write a simple C program to demonstrate the use of a static variable.

4. What do you mean by ternary operator? Write a simple C program that makes use of the ternary operator.

5. Describe the syntax of `else if` ladder. How is it different from a `switch` statement?

6. Write a function called `is_divisible` that takes two positive integers as arguments. Assume the first argument is less than the second. The function returns 1 if the second is divisible by the first, otherwise 0. Example: `is_divisible(3,7)` returns 0. No main function needed.

7. What is a pointer variable? How is pass by reference advantageous compared to pass by value?

SECTION “C”


8. Differentiate between array and pointer. In what ways are they similar? Write a complete C program demonstrating the addition of two-dimensional arrays.

9. What are the advantages of structures in C programming?  
   a. Create a structure `Point` with attributes `x_axis` and `y_axis`.  
   b. In the main program, create an array of 10 points. Write code to input data for the 10 points.  
   c. Check whether any point entered is (0,0).

10. Write a complete C program demonstrating the use of library functions: `fopen()`, `fclose()`, `fgetc()`, and `fputc()`.



KATHMANDU UNIVERSITY
End Semester Examination
2017

Course: COMP 103
Year: I Semester: I
SECTION “B”


1. What do you mean by data types? Explain the difference between `'a'` and `"a"` when used as constants in C. Describe their memory representation.

2. What is the difference between a local and a global variable in C? Write a program illustrating the use of a static variable.

3. What do you mean by pass by value? Illustrate with an example how passing an array differs from passing a variable to a function.

4. Triangle Inequality Theorem: the sum of two sides of a triangle must be greater than the third side. Write a program that takes three sides of a triangle and prints whether it is valid.

5. Two keywords frequently used in looping are `break` and `continue`. Write a C program demonstrating their use.

6. Write a function called `how_many_primes_between` that takes two positive numbers. Assume the first argument is less than the second. The function returns the number of prime numbers between them. Example: `how_many_primes_between(3,7)` returns 1 (prime number 5).

7. What are logical operators? Describe each one. Provide a C snippet demonstrating the `!` (not) operator.

SECTION “C”


8. Write a program that reads a 4 x 4 matrix and prints the sum of each row. Example output:  
1 2 3 4 Sum = 10
5 6 7 8 Sum = 26
9 10 11 12 Sum = 42
13 14 15 16 Sum = 58

9. Create a structure called `Student` with attributes: `name`, `age`, `birth_place`. Create an array of 10 students and input their data. Check whether any two students are neighbors (same birth place).

10. Differentiate between `malloc` and `realloc`. Write a complete C program illustrating their use including `free()`.



KATHMANDU UNIVERSITY
End Semester Examination
2018

Course: COMP 103
Year: I Semester: I
SECTION “B”


1. Discuss the characteristics of C programming language. Describe general programming rules.

2. What do you mean by an expression? With example, describe different types of operators used in C programming.

3. Define `switch-case` in C. Considering character constants as test values, write a program illustrating `switch-case`, `continue`, and `break`.

4. Write a C program to check whether a user-given number is Armstrong using a function.  
[Hint: XYZ is an Armstrong number because X³ + Y³ + Z³ = XYZ]

5. Write a program that reads ten numbers and displays if any number is repeated.

6. Mention the significance of functions. Illustrate different features of functions in C.

7. What do you mean by storage class? Using a C program example, differentiate static storage class from automatic storage class.

SECTION “C”


8. Using a function, write a program to calculate the transpose of a user-given M x N matrix. Display the resultant matrix after adding 5 to each element.

9. How is a structure different from a union? Assume a structure has five members: `Roll_Num`, `Name`, `Dept`, `DOB`, and `Marks`. `DOB` contains `Day`, `Month`, and `Year`. Using a nested structure, write a C program to enter records of `n` students and display them in descending order based on name.

10. Define dynamic memory allocation. Briefly explain functions used in it. Write a C program to calculate average marks of `n` students in COMP 103 using `malloc()`.



KATHMANDU UNIVERSITY
End Semester Examination
2019
Course: COMP 103
Year: I Semester: I
SECTION “B”

1. How can an array be declared in C? Briefly explain datatype, identifier, and variable with proper examples.

2. Write a C program to count the total number of prime numbers stored in an array using a function. Explain the program in comments.

3. What is associativity of an operator? Discuss briefly about a type of operator with right-to-left associativity.

4. Using if…else ladder, write a program to compute and output the number of digits in a user input number:  
   - 0–9 → 1 digit  
   - 10–99 → 2 digits  
   - 100–999 → 3 digits  
   - >999 → above 3 digits  
   - Otherwise → Invalid input

5. Write a program to read a binary number and convert it into decimal. Example: 111 = 1*2² + 1*2¹ + 1*2⁰ = 7.

6. Write short notes:  
   a. Advantages of modularization in C  
   b. Unformatted I/O functions  
   c. Pointer to structure

SECTION “C”

8. Differentiate between dynamic memory allocation and automatic memory allocation in an array. Explain DMA functions with proper syntax in C.

9. How does recursion consume more memory and execution time than iteration? Explain with an example. Write a recursive program to find the sum of n natural numbers (user input).

10. Define a structure `Distance` with two entities `km` and `m` (both int). Write a program to take five distances as input and display them in ascending order.  
    Write a function `void swapDistance(Distance *a, Distance *b)` to swap two distance values.



KATHMANDU UNIVERSITY
End Semester Examination
2022
Course: COMP 102
Year: I Semester: I
SECTION “B”

1. How do `getchar()` and `putchar()` differ from `scanf()` and `printf()`? Explain with examples.

2. Compare `if-else` statement with conditional operator `?:` including syntax.  

3. Write a program using a pointer to read an array of integers and print its elements in reverse order.

4. How can header files be accessed in C? Explain with examples.

5. Write a C program to print numbers that do not appear in the Fibonacci series. Number of elements to be printed should be user input.

6. How does a C program behave when a variable inside a function has the same name as the function? Discuss scope of global and local variables with examples.

7. Write a C program to convert temperature from Celsius to Fahrenheit. Formula: F = 9C/5 + 32. Describe each term.

SECTION “C”


8. Write a C program that performs matrix multiplication:  
   a. Read elements of two matrices n x m from user.  
   b. Check whether multiplication is possible; if not, display a message.  
   c. Pass arrays to a user-defined function.  
   d. Perform multiplication in user-defined function.  
   e. Display the result.

9. Explain with example how a library function differs from a user-defined function. Write a C program to generate prime numbers from 1 to 100 and pass them to a function `primeAverage` that calculates the average.

10. Describe the properties of four built-in data types in C. With proper syntax and examples, demonstrate how a user-defined data type can be created and used.



KATHMANDU UNIVERSITY
End Semester Examination
July, 2022
Course: COMP 102
Year: I Semester: I
SECTION “B”


1. Explain precedence and associativity in expression evaluation with examples. Define left-to-right and right-to-left associativity.

2. Compare `for` loop with `do-while` loop. Write a program that displays your name if an integer is divisible by 7 but not by 10.

3. Write a C program to calculate the area of a triangle using Heron’s formula:  
area = sqrt(s * (s-a) * (s-b) * (s-c))
s = (a + b + c) / 2
Read a, b, c in main and pass them to a user-defined function.

4. Illustrate the use of `auto` and `static` keywords with examples.

5. Define the term structure. Write a program that stores 5 values in a structure:  
struct record {
int emp_id;
char name[20];
float salary;
}


6. What is recursion? Explain recursive process and illustrate the working mechanism with sum of natural numbers.

7. Write a C program to input a number and find the largest digit. Read the number in main and pass it to a user-defined function.

SECTION “C”

8. a. Explain the purpose of functions. Differentiate between pass by value and pass by reference.  
b. What are arguments? Explain their importance with examples.  
c. What are return types? Explain their importance with examples.

9. a. Discuss a menu-driven program with an example.  
b. Write a modular program:  
   i) Find factorial of an integer entered via keyboard  
   ii) Find the square of a number entered via keyboard  
   iii) A function `power(a,b)` to calculate a^b  

Write a program that defines a structure `student` with attributes `name`, `roll_no`, `email`, `enrolled_year` etc. Read data for at least five students. Access structure members using a pointer and display records in ascending order by `enrolled_year`.


KATHMANDU UNIVERSITY
End Semester Examination
2024
Course: COMP 102
Year: I Semester: I
SECTION “B”


1. What is an identifier? What are the ways to assign values to a variable? Define constant and its types.

2. List different operators with their associativity in C. What are relational and assignment operators? Explain with examples.

3. Explain basic structure of a C program with examples. Write syntax and examples of `puts()`, `scanf()`, `printf()`, and `putchar()`.

4. Briefly describe different types of functions available in C. Write a C program to input a number and calculate the sum of its digits using recursion.

5. Write a C program to input a list of values into an array. Pass the list to a function to sort in ascending order and display the sorted list from main.

6. Differentiate between `malloc()` and `calloc()` with examples.

7. Explain any two storage classes. Write a C program to input a 2x2 matrix and display its transpose.

SECTION “C”


8. Write a menu-driven program in C with options:  
a. Check if a number is strong (e.g., 145 → 1!+4!+5! = 145)  
b. Check if a number is prime or composite  
c. Display the middle number among three numbers  
d. Exit

9. What is `typedef`? Write a program to read a structure `collage` with `name`, `estDate`, and `location` where `estDate` is another structure with `day`, `month`, `year`. Display records of any number of colleges entered by the user.

10. a. Write advantages of pointers. Write a program using pointers to swap values of two variables via a separate function.  
 b. Explain `break` and `continue` statements with examples.
